# Render Blueprint: AgentTag canvas (static Next app, no database)
services:
  - type: web
    name: agenttag-web
    runtime: node
    plan: starter
    region: oregon
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
